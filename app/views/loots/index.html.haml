%h1 Listing Loots

%table
  - for loot in @loots
    %tr
      %td= loot.toon.name
      %td= loot.primary ? "Primary" : "Secondary"
      / %td= link_to loot.item.name, wowhead_url(loot.item.wow_id)
      %td= link_to loot.item.name, item_path(loot.item)
      %td= link_to "Edit", edit_loot_path(loot)